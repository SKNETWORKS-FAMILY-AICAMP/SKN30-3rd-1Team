# Never commit these. scripts/check-scope.sh also blocks them at the
# baseline checkpoint, but .gitignore is the first line of defense.
.env
.env.*
*.pem
*.key
id_rsa*
credentials.json

# Workflow scratch. Task documents themselves SHOULD be committed;
# logs are noisy and machine-specific.
.agent-workflow/tasks/*/logs/
